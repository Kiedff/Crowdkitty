<!DOCTYPE html>
<html>
<head>
  <title>CrowdKitty</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <header class="navigation" role="banner">
    <div class="navigation-wrapper">
      <nav role="navigation">
        <ul class="centered-navigation-menu show">
        <li class="nav-link"><%= link_to 'Home', root_path %></li>
        <li class="nav-link"><%= link_to 'About Us', '/pages/about' %></li>
        <% if can? :create, Project %>
          <li class="nav-link"><%= link_to 'Create Project', new_project_path %></li>
        <% end %>
          <li class="nav-link"><%= link_to 'Discover Projects', projects_path %></li>
          
          <li class="nav-link"><%= link_to 'Search Projects', categories_path %></li>
          <% if user_signed_in? %>
            <li class="nav-link">
              <%= link_to "Profile", User.find(current_user.id) %> 
              <%= link_to "Logout", destroy_user_session_path, method: :delete %> 
            </li>
            <% else %>
            <li class="nav-link">
              <%= link_to "Login", new_user_session_path %>    
            </li>
            <li class="nav-link">
              <%= link_to "Register", new_user_registration_path %>
            </li> 
          <% end %>
        </ul>
      </nav>
        <div class="navigation-tools">
          <div class="search-bar">
            <form role="search" action="/projects" placeholder="search projects" >
              <input type="search" placeholder="Search projects" name="search" id="srch-term">
              <button type="submit"><img src=<%= image_path "search-icon.png" %> alt="Search Icon"></button>
            </form>
          </div>
        </div>      
    </div>
  </header>

  <main>
    
  </main>
  
  <footer>
    
  </footer>

  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>

  <%= yield %>

</body>
</html>
